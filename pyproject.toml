[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "perspective-dynamics"
version = "0.1.0"
description = "Reproducible computational experiments on perspective-dependent associative dynamics"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
