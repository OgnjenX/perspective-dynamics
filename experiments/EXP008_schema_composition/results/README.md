# Results

`events.jsonl` preserves one audit event per condition, including plan provenance. `summary.json` records the frozen outcome and claim boundary. Both are generated by `run.py`; no negative result is discarded.
