# Conclusion

The prospective run passed H8.1–H8.3 on the toy task. Structured composition reached the target in three explored states, using the perspective sequence `support → stability → goal`. The support step produced `elevated(platform)`, which enabled the stability step; the resulting `stable(platform)` enabled the goal step.

The single-perspective and pooled-no-composition controls failed under the same initial facts. This is an implementation demonstration of the proposed operation, not evidence for learning, neural dynamics, biological plausibility, or human creativity. Held-out renaming, schema-order, and matched-capacity experiments remain required.
