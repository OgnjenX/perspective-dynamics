# Results

`summary.json` records frozen criteria, predictions, resets, category counts,
and the explicitly post-hoc matched-capacity SOM diagnostic.
`random_scores.json` preserves all 200 random-category outcomes.
