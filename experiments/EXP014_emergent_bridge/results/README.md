# Results

`summary.json` records candidate predictions, the learned positive-category
intersection, removal tests, controls, model capacity, and frozen criteria.
`random_scores.json` preserves all 200 random-control outcomes.
