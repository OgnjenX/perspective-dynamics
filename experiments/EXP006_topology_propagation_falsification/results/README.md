# EXP006 Results

Generated artifacts:

- `raw.csv`: all scheduled runs;
- `summary.csv`: environment × threshold × block summaries;
- `optima.csv`: selected block for every environment × threshold;
- `criteria.csv`: frozen criterion outcomes by environment and overall;
- `integrity.csv`: graph and reproducibility checks.

The committed files were generated only after the design (`3752fa7`) and implementation (`5dc2de4`) were committed.
